# THREAD - Render.com Environment Variables Template
# Copy these to Render dashboard (Environment Variables section)

# Server
PORT=10000
HOST=0.0.0.0
NODE_ENV=production

# Database (using persistent disk)
DATABASE_PATH=/data/thread.db

# Images (using persistent disk)
IMAGE_STORAGE_PATH=/data/images

# Ollama (optional - leave empty for local-only operation)
# Set to your external Ollama URL if you have one hosted
OLLAMA_BASE_URL=
DEFAULT_VISION_MODEL=llava:7b
DEFAULT_TEXT_MODEL=llama3.2:3b

# Security (JWT_SECRET auto-generates on Render)
JWT_SECRET=  # Auto-generated
API_KEY_PREFIX=thread_sk_

# Features (simplified for single-user)
ENABLE_WEBHOOKS=false
ENABLE_MULTI_USER=false

# Weather
WEATHER_API_BASE=https://api.open-meteo.com/v1

# Optional
# GOOGLE_API_KEY=your-google-api-key
